policies:
- ../map-with-userinfo.yaml
resources:
- resources.yaml
mapTestSuites:
- policy: userinfo-policy
  binding: userinfo-policy-binding
  tests:
  - object: 
      kind: Deployment
      name: base
    expect: mutate
    expectObject:
      kind: Deployment
      name: mutated
    userInfo:
      name: "system:serviceaccount:foo:default"
